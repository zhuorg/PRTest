name: try docker action
on:
  check_suite:
jobs:
  example:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        package: [identity, db, api, ui]
    steps:
    
    - name: test dockercontainer_action
      run: echo hello
