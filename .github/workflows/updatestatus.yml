name: update status

on: 
  push:
    paths: 
      - '.github/workflows/updatestatus.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: mask
      run : | 
        chmod +x .github/mask.sh
        .github/mask.sh
        echo "Mona The Octocat" 
      
#     - name: Setup tmate session
#       uses: mxschmitt/action-tmate@v2  
#     - run: |
#         ssh -vvv localhost -p 22 -o StrictHostKeyChecking=no
 #ssh -vvv localhost -p 22 1234

  
