name: add comment
on: 
  push:
    paths:
      - '.github/workflows/TestPR.yml'

jobs:

  job2:
    runs-on: macos-latest
    steps:
    - name: check simulator
      run: xcrun simctl list
      
         
     
